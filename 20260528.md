 今天完成总结：

  - 患者管理主链路继续拆解并稳定：
      - 修复执行跟踪页右侧空白问题。
      - 抽出 useFollowupTasks。
      - 抽出 useReportFollowupTasks.
      - 抽出 usePatientManagementNavigation.
      - 抽出 useFollowupTrackingView.
      - PatientManagementView.vue 已从 3000+ 行降到约 1285 行。
  - 随访工作流配置页拆解：
      - FollowupWorkflowView.vue 从 1052 行降到约 557 行。
      - 新增：
          - useFollowupWorkflowConfig
          - useFollowupTemplates
          - useFollowupTemplateNodes
          - useFollowupKnowledge
          - useFollowupAiRules
  - 前端性能和结构优化：
      - 路由改为懒加载。
      - 构建大包 warning 已消失。
      - 新增公共 API helper：src/utils/apiClient.js
      - App.vue 增加懒加载页面 fallback。
  - 自动验收增强：
      - npm run smoke
      - npm run smoke:ui
      - npm run smoke:workflow-ui
      - npm run smoke:workflow-save-ui
      - npm run smoke:all-ui
      - 新增 API client smoke。
      - 新增 workflow 配置页 UI smoke。
      - 新增 workflow 保存类 UI smoke：会新建模板、节点、知识、AI 规则，并自动清理。
      - UI smoke 失败时会保存截图。
  - 已提交的关键提交：
      - 8c39421 fix: restore follow tracking layout
      - 5c0c660 refactor: split followup task state
      - 184b9f0 test: assert follow tracking layout in UI smoke
      - 6d62107 refactor: split report followup task logic
      - 3ff69c0 refactor: lazy load routes and split navigation state
      - a2b58d9 test: capture followup smoke failures
      - eedd666 refactor: share API client and tracking view state
      - f8a41db refactor: split followup workflow config logic
      - 26ed220 test: add followup workflow UI smoke
      - ab4e10b test: expand workflow and API smoke coverage
      - 8ef4c53 test: add workflow save UI smoke

  当前状态：

  - 自动验收全部通过：
      - npm run smoke
      - npm run smoke:all-ui
      - git diff --check
  - 前端服务：http://127.0.0.1:5173/login
  - 登录账号：smoke_admin / Smoke@123456
  - 工作区只剩未跟踪的 20260528.md，代码工作区干净。

  明天建议继续方向：

  1. 先跑一次回归

     cd demo/vue-prototype
     npm run smoke
     npm run smoke:all-ui

  2. 继续做真实保存类覆盖
     优先补患者管理侧的保存类 E2E：
      - 患者建档
      - 影像上传/删除
      - 报告审核
      - 创建首次随访
      - 企微绑定/解绑
  3. 统一错误提示
     抽 useToast / formatApiError，把各 composable 里的错误提示风格收敛。
  4. 准备交付说明
     写清楚：
      - 已完成能力
      - 验收命令
      - 手动入口
      - 已知风险
      - 后续待办
  5. 如果继续拆组件
     不建议按行数硬拆。只有当某块 UI 复用或频繁修改时，再拆 template 组件。


是，现在适合做阶段总结。

  当前项目状态

  B 端 Vue 原型的核心闭环已经基本可交付演示：

  患者建档 -> 影像资料维护 -> 报告生成/审核 -> 创建首次随访 -> 公开打卡 -> 执行跟踪

  这轮完成的重点

  - 补齐患者管理侧保存类 UI smoke：
      - 患者建档保存
      - 报告审核归档
      - 影像上传/删除
      - 审核后创建首次随访
      - 多结节建档保存
      - 三合并报告生成与审核
  - 修复报告审核流程：
      - 前端现在按后端要求执行 保存草稿 -> 提交审核 -> 审核通过
  - 抽出 smoke 公共工具：
      - smoke_utils.py
  - 新增交付文档：
      - demo/vue-prototype/DELIVERY.md
  - 纳入进展记录：
      - 20260528.md

  已验证通过

  npm run smoke
  npm run smoke:all-ui
  git diff --check

  其中 smoke:all-ui 已覆盖：

  - 建档保存
  - 报告审核
  - 影像上传/删除
  - 审核后首次随访
  - 多结节保存
  - 三合并报告审核
  - 随访工作流 UI
  - 随访工作流保存
  - 报告到随访再到公开打卡的完整链路

  已提交

  最新 4 个提交：

  3d4717d docs: add May 28 progress notes
  6a8d5d6 docs: add vue prototype delivery notes
  3b2a460 test: add patient workflow UI smoke coverage
  8d281c4 fix: submit report advice before approval

  当前分支：

  feat/vue-prototype-updates

  本地领先远端 19 个提交，工作区干净。

  剩余建议

  下一步主要不是继续堆功能，而是：

  1. 推送当前分支
  2. 给领导/同事按 DELIVERY.md 走一遍演示
  3. 视反馈再补：
      - 执行跟踪页强校验
      - 统一错误提示
      - 快速 API smoke
      - 企业微信真实发送，等条件具备后再做
